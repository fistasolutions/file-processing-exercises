# Task Tracker

A lightweight task tracking API built with Flask.

## Setup

```bash
pip install -r requirements.txt
python src/app.py
```

## Project Structure

- `src/` - Application source code
- `src/models/` - Data models
- `tests/` - Test suite
- `config/` - Configuration files
