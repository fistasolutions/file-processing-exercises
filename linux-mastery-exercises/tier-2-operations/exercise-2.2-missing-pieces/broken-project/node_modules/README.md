# Simulated node_modules
This directory represents a 200MB node_modules folder with 15,000 files.
In the real scenario, this would be actual npm packages consuming disk space unnecessarily.
The agent is Python-based — node_modules should not be here.
